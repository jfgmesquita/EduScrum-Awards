package com.group7.eduscrum_awards.model.enums;

/** Defines who receives the award. */
public enum AwardScope {
    STUDENT, // Individual award
    TEAM     // Team award (but assigned to members individually)
}